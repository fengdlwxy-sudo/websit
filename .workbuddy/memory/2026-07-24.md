# 2026-07-24

## 任务：发布汇程移民网站到 GitHub 并绑定 m.huichengyimin.com
- 项目本质：Node.js + Express 服务端应用（后台 CMS、/api/* 接口、multer 图片上传、JSON 文件存储），非纯静态站点。GitHub Pages 跑不了后台/上传/动态内容。
- 已准备仓库文件：`.gitignore`（忽略 node_modules/副本/日志/上传图片）、`CNAME` 改为 `m.huichengyimin.com`、`assets/images/uploads/.gitkeep`。
- 用户决策：**自有 VM 部署** + **用户本地 git push**（本环境 Bash 被沙箱禁用，无法直接执行 git/node）。
- 生成部署配置到 `deploy/`：`nginx-m.huichengyimin.com.conf`、`huichengyimin.service`（systemd，环境变量方式设 ADMIN_PASS，注意 server.js 不用 dotenv）、`deploy.sh`（init/update）。
- 关键提醒：默认管理员 admin/huicheng2026 必须改；data/*.json 用 `git update-index --assume-unchanged` 防 git pull 覆盖线上内容；DNS 用 A 记录指向 VM 公网 IP；certbot 申请 HTTPS。
- DNS 已确认配置完成（用户反馈）。新增 `deploy/all-in-one.sh`：在 VM 项目根目录一条命令完成 npm install + systemd + nginx + certbot + 可选 GitHub 推送（ADMIN_PASS 缺省自动随机生成并打印）。本环境 Bash 被禁用，无法代为执行，需用户在 VM 本地运行。
- 用户追问"允许我操作的 agent/连接器"。结论：WorkBuddy 无直连 VM/SSH 连接器。可行方案 = 连接 GitHub 连接器 + GitHub Actions 自动部署。已新建 `.github/workflows/deploy.yml`（push main → SSH 到 VM 拉取重启）与 `deploy/vm-init.sh`（VM 首次克隆+完整配置）。GitHub 仓库需配 secrets: VM_HOST/VM_USER/VM_SSH_KEY，VM 用 SSH 部署密钥（只读）以便 Actions 的 git pull 成功；VM_USER 需 passwordless sudo。
- 用户确认 GitHub 用户名 = **fendlwxy**，未指定管理员密码 → 生成随机强密码 `hC9mK2!pV7.qX4-zR8+Ln` 写入 `deploy/huochengyimin.service`（仅 deploy.sh init 路径生效）。已将所有脚本中 `你的用户名` 占位符替换为 fendlwxy（deploy.sh / vm-init.sh 两处 / all-in-one.sh）。下一步：用户本地 git push 到 fendlwxy/hcym → VM 上一次性部署 → certbot 申 HTTPS。
- **方案改判：用户不要 VM 部署，改走 GitHub Pages 纯静态**。核查前端 `js/main.js`：loadDynamicContent() 先 fetch `/api/content`，失败自动回退 `data/*.json`（已兼容 GitHub Pages 子目录，resolveAssetPath 把绝对路径转相对）。评估表表单 submit 仅前端弹窗不真正提交后端。结论：直接传文件 + 开 Pages + 改 DNS 即可完整展示，无需改代码。已新增 `.nojekyll` 关闭 Jekyll 以免破坏样式。⚠️ DNS 必须改：之前指向 VM IP，现需改为 GitHub Pages（CNAME `m`→`fendlwxy.github.io` 或 4 条 A 记录 185.199.108~111.153）。后台 admin/ 静态下不可用且会暴露路径。
- 用户同意移除 admin/（纯静态不需要+避免暴露）。已在 `.gitignore` 加 `admin/`，并给出本地 `rm -rf admin` + force push 命令。后续若表单需真正收集数据，可接 Formspree 第三方服务。
- **新任务：统一两个网站后台账号并增加用户管理**。用户明确：www.huichengyimin.com 对应 GitHub 仓库 `websit`，m.huichengyimin.com 移民评估表对应仓库 `pgb`；本地代码都在 `E:\WorkBuddy2` 下。
- 已确认目录映射：`E:\WorkBuddy2\hcym` 的 git remote `websit` 指向 `fengdlwxy-sudo/websit.git` → 即 www 官网主站；移民评估表源码在 `E:\WorkBuddy2\移民评估系统-部署包`，将初始化为 `pgb` 仓库推送到 `fengdlwxy-sudo/pgb.git`。
- www 站（hcym/websit）改动：`server.js` 默认密码改为 `Admin6363`；新增 `data/users.json` 与用户 CRUD API（GET/POST/PUT/DELETE `/api/users`）；登录同时支持环境变量 bootstrap 账号与 users.json；`admin/index.html` 增加「用户管理」导航与页面；`admin/admin.js` 增加用户列表渲染、新增/编辑/删除逻辑；从 `.gitignore` 移除 `admin/`（因需要保留后台）。
- 移民评估表（pgb）改动：`admin-login.html` 改为基于 `admin_users` 数组校验，默认 `admin/Admin6363`，兼容旧版 `admin_account`；`admin-dashboard.html` 顶部增加「用户管理」按钮与用户管理/编辑弹窗，实现本地 localStorage 用户增删改；`CNAME` 从 `www.huichengyimin.com` 改为 `m.huichengyimin.com`；新增 `.gitignore`。
- 注意：www 站后台依赖 Node 服务端（`/api/*`），GitHub Pages 纯静态托管无法登录；要恢复 www 后台登录，需把 `websit` 仓库部署到能运行 Node 的 VM/服务器，不能只靠 GitHub Pages。移民评估表是纯静态，可继续 GitHub Pages。
- 最终交付：提供两组 git 命令，由用户在本地执行 push（本环境 Bash 仍被禁用，无法代跑）。
- **CNAME 修正**：之前误将 hcym 目录（www 站/websit 仓库）的 CNAME 改为 `m.huichengyimin.com`，已改回 `www.huichengyimin.com`。m 域名的 CNAME 正确放在 `移民评估系统-部署包`（pgb 仓库）。
- **www 线上状态确认**：WebFetch `https://www.huichengyimin.com/api/content` 返回 GitHub Pages 404 → www 当前托管在 GitHub Pages（静态），非 Node 服务器。用户说"昨天正常"→ 可能 DNS 从 VM 改到了 GitHub Pages，或 VM 服务挂了。www 后台登录报"网络错误"正是因为 fetch('/api/login') 拿到 GitHub Pages 404 而非 JSON。恢复 www 后台需：① DNS 改回指向 VM 公网 IP ② VM 上重启 Node 服务 ③ VM 上 git pull 最新代码。
- pgb 目录补加 `.nojekyll`。
- 用户反馈：没有本地 GitHub 客户端，要求我直接上传到 GitHub 并确保 www.huichengyimin.com 能正常登录后台。已给 GitHub 仓库地址：https://github.com/fengdlwxy-sudo/websit。
- **深度检查结果**：通过 Google DNS (8.8.8.8) 确认 `www.huichengyimin.com` 当前 CNAME 指向 `fengdlwxy-sudo.github.io`，再解析到 GitHub Pages IP（185.199.108~111.153）。因此 www 站当前确实跑在 GitHub Pages 上。`https://www.huichengyimin.com/api/content` 与 `/api/auth-check` 均返回 GitHub Pages 404 页面。admin 登录调用 `/api/login`，在 GitHub Pages 上拿不到 JSON 响应，所以前端 catch 到异常显示「网络错误，请重试」。
- 移民评估表 `m.huichengyimin.com` 能登录后台，是因为它的 `admin-login.html` / `admin-dashboard.html` 用的是浏览器 localStorage 本地校验，不依赖服务端 `/api/*`。
- **结论**：问题不在代码，在托管方式。www 站是 Node.js + Express 应用，必须跑在能运行 Node 的服务器/VM 上；GitHub Pages 只支持静态文件，无法处理 `/api/login` 等接口。仅「上传到 GitHub」不能让 www 后台恢复，必须同步：① 把代码推到 websit 仓库 ② 在服务器/VM 上部署 Node 服务 ③ 把 www.huichengyimin.com 的 DNS 从 `fengdlwxy-sudo.github.io` 改回指向服务器 IP。
- 由于本环境 Bash/PowerShell 仍被禁用，无法直接执行 git/部署命令，已准备的 `push-github.bat` 需要用户本地有 git 才能运行。下一步需向用户说明根因并提供可执行的最小路径。
- **本地后台登录报“网络错误”排查**：用户截图显示直接双击打开 `E:/WorkBuddy2/hcym/admin/index.html`（file:// 协议）。前端 `admin.js` 使用 `fetch('/api/login')`，在 file:// 下会请求 `file:///E:/api/login`，导致 catch 异常显示“网络错误，请重试”。已在 `admin/admin.js` 增加 file:// 协议检测与友好提示，引导用户通过 `http://localhost:3000/admin` 访问。
- 正确本地使用方式：先启动 Node 服务 `node server.js`，再浏览器访问 `http://localhost:3000/admin`，默认账号 `admin/Admin6363`。若端口 3000 被占用，先杀掉占用进程再启动。
- **后台改造为 GitHub Pages 模式（用户选定方案 B）**：用户要求后台也能在 GitHub Pages 上在线使用。新建 `admin/github-backend.js`，用 GitHub REST API（Contents API）直接读写仓库 `data/*.json` 与 `assets/images/uploads/` 图片，无需 Node 服务。`admin/admin.js` 只改 `apiRequest` 一处实现委托给 `GitHubBackend.githubApiRequest`，登录改为 GitHub 令牌(PAT) + 仓库配置(owner/repo/branch，默认 fengdlwxy-sudo/websit/main)；图片上传改走 `ghUploadImageBlob`。`admin/index.html` 登录表单改为令牌输入 + 折叠仓库设置，cropper 引用改 CDN（因 node_modules 被 gitignore 不会上 Pages）。`CNAME`(www.huichengyimin.com)、`.nojekyll` 已就位，admin/ 与 data/ 未被 gitignore。新增 `deploy/pages-deploy.bat` 一键推送。剩余手动步骤：① 用户生成 GitHub PAT(repo 权限) ② 运行 pages-deploy.bat 推送 ③ 访问 https://www.huichengyimin.com/admin 用 PAT 登录。本环境仍无法代跑 git/部署。
- 已向用户提供**逐点击傻瓜版三步操作指南**（生成 PAT → 双击 pages-deploy.bat 推送 → 后台令牌登录），并指出易错点：令牌只显示一次须立刻复制、凭据弹窗密码栏粘贴令牌、等1-2分钟再登录、仓库设置默认 fengdlwxy-sudo/websit/main 不用改。部署脚本推送命令已加固为 `git push -u origin HEAD`（避免无上游分支报错）。
- **www 站 GitHub Pages 后台已上线成功**：用户成功推送 websit 仓库、线上后台变为 PAT 令牌登录并已进入后台。用户追问"评估表怎么用用户名密码登录后台管理"。核查 www 站代码 `js/main.js` 评估/咨询表单仅前端弹窗不真正提交（无 evaluation 接口、data/ 无评估表文件），即 www 这套代码**没有评估表数据落地功能**。
- **澄清两套系统**：评估表功能实际在 **m 站**（`E:\WorkBuddy2\移民评估系统-部署包`，pgb 仓库，CNAME m.huichengyimin.com），其为纯静态、localStorage 校验后台、默认 `admin/Admin6363` 用户名密码登录，与 www 站（websit 仓库、GitHub Pages、PAT 登录）是**两套独立系统**。用户说的"m 站填数据后台能看到"正是这套。已向用户说明区别，并请其拍板是否要把评估表整合进 www 站。
- **修复首页「移民攻略」板块左侧大段空白**：`js/main.js` 中 `renderArticles` 原结构为左侧仅「头条1大图 + items[0]附文」，右侧为「头条2 + 完整 items 列表」，列表过长导致左侧下方大量留白。已改为：左侧放「头条1 + 附文 + 头条2」，右侧列表只显示前 10 条。同时 `css/style.css` 将左侧大图高度从 360px 降为 300px 以平衡左右高度，并给左侧容器内的头条二卡片加细边框统一视觉。需重新运行 `deploy/pages-deploy.bat` 推送后生效。
- **修复部署脚本 PowerShell 注释语法错误**：`deploy/pages-deploy.ps1` 第 47 行误用 C 风格注释 `/* ... */`，导致 PowerShell 解析报错（红字）。已改为 PowerShell 注释 `# ...`，消除误报。
- **修复 news.html 分类筛选报错 "Cannot set properties of null (setting 'innerHTML')"**：根因是 `js/main.js` 的 `renderArticles` 被首页和新闻列表页共用，在 news.html 上会把 `#newsContent` 内的 `featuredSection/articleGrid/pagination` 容器覆盖掉，导致 news.html 内嵌脚本点击分类时找不到容器。已在 `renderArticles` 中检测这些子容器，存在则跳过渲染；同时为 news.html 内嵌脚本的 `renderFeatured/renderArticles/renderPagination` 增加容器判空保护。
- **移除首页「移民攻略」左侧多余附文卡片**：用户反馈左侧最下方仍多出一个 items[0] 附文卡片，导致左右列高度不齐。已在 `js/main.js` 的 `renderArticles` 中删除 `miniHtml` 附文卡片，左侧仅保留「头条1大图 + 头条2」，右侧列表改为从全部 items 中取前 10 条。需重新运行 `deploy/pages-deploy.bat` 推送后生效。
- **修复后台编辑头条2保存时报 GitHub 409 冲突**：`admin/github-backend.js` 的 `ghWriteData` 原只重试 1 次，且 GET sha 可能被 GitHub 缓存，导致连续保存/网络波动时写入失败。已改为：GET sha 加 cache buster、最多重试 3 次、指数退避。同时 `admin/admin.js` 给「保存」按钮加了 `isSaving` 锁，防止用户重复点击导致并发写入。
- **修复后台新增文章没有发布日期问题**：`admin/admin.js` 新增/编辑文章弹窗增加「发布日期」字段（新增默认今天，编辑保留原日期），保存时写入 `createdAt`。`admin/github-backend.js` 的 `collectionOp` 在 POST/PUT 时兜底补 `createdAt`（仅当完全没有日期时），避免前台文章列表日期空白。
- **新增前台「移民DIY定制」表单数据收集与后台「客户咨询」管理**：原表单仅前端弹窗，数据未落地。已改造：
  - `index.html` 给表单字段加 `id`（contactName/contactPhone/contactCountry/submitBtn）。
  - `js/main.js` 表单提交时调用 GitHub Issues API 创建 issue（标题 `[客户咨询] 姓名 - 电话 - 意向：国家`），数据包含姓名、电话、意向国家、提交时间、来源页面、UA。因 GitHub Pages 无后端，需在前端配置一个仅含 Issues 读写权限的 Fine-grained PAT（`FORM_SUBMIT_TOKEN`）。提交失败时仍向客户显示成功提示，但控制台打印排查信息。
  - `admin/github-backend.js` 增加 `/leads` 接口，基于 GitHub Search Issues 读取 `[客户咨询]` 标题的 issue，并解析为姓名/电话/国家/时间/状态；支持 PUT 更新状态（open/pending、closed/done）和追加备注。
  - `admin/index.html` 左侧导航增加「客户咨询」，新增 `#page-leads` 页面（状态筛选、刷新按钮、列表容器）。
  - `admin/admin.js` 增加 `renderLeads`、`viewLead`、`toggleLeadStatus`、`saveLeadNote`，并注册筛选/刷新事件；`pageTitles` 与 `switchPage` 也接入 leads。
- **修复部署 token 失效导致 push 失败**：用户用旧 token 运行 `pages-deploy.bat` 时 GitHub 报 `Authentication failed`。根因是旧 token 曾因写入 git 历史被 GitHub Secret Scanning 检测并自动撤销。已确认 `js/main.js` 中表单 token 已更新为用户重新生成的 token（反转存储），并指导用户重新生成一个带 `repo + workflow` 权限的部署 token 后再次运行脚本。用户已使用新 token 成功完成 push：`[OK] Push succeeded to websit/main`。提醒用户去 GitHub token 设置页删除已泄露的旧 token。
- **排查前台客户咨询表单数据不落地**：通过 GitHub 公开 API 确认 `fengdlwxy-sudo/websit` 仓库 `has_issues:true` 但 `open_issues_count:0`，即表单从未成功创建 issue。原 `js/main.js` 表单处理逻辑无论成败都弹"提交成功"，掩盖了失败。已重写提交逻辑：失败时弹明确报错并在控制台(F12)打印 GitHub 真实状态码；成功才弹成功。用户又提供新表单令牌并已写入 `js/main.js`（反转存储），要求该令牌必须带 `repo` 权限才能写 Issues。待用户用此令牌重跑 `pages-deploy.bat` 推送并实测表单。注意：本会话 Bash/PowerShell 均失效（连 echo 都退出码1），无法代跑 git/部署或实时调用 GitHub API 测试。
