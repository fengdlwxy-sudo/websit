# 2026-07-23 工作日志

## 完成的工作

### 后台上传功能修复（用户反馈截图）

用户截图反馈后台"所有需要上传图的模块"不能正常使用，并要求在选择好图片后，旁边添加"上传"功能按钮，能看到图片被上传了然后再确认发布。

#### 1. 修复 `getRichEditorContent` 未定义错误
- 文件：`admin/admin.js` 第 1711 行
- 原因：保存案例时调用了不存在的 `getRichEditorContent()`，实际函数名是 `getRichEditorHtml()`
- 修复：将 `getRichEditorContent()` 改为 `getRichEditorHtml()`

#### 2. 图片字段改造为两步上传流程（核心需求）
- 文件：`admin/admin.js` 的 `imageFieldHtml`、`handleImageFieldSelect`、`confirmImageFieldUpload`、`setImageFieldValue`、`clearImageField`
- 改动：
  - "📤 上传图片"按钮 → 拆分为 "📁 选择图片" + "☁️ 上传" 两个独立按钮
  - 选择文件后：用 ObjectURL 在预览区显示**待上传**的图片，并显示橙色"待上传"标签
  - 旁边显示文件名和大小："已选择: xxx.jpg（123.4 KB）"
  - 点击"☁️ 上传"按钮才弹出裁剪器、确认后上传
  - 上传成功后：预览区显示绿色"已上传"标签，3秒后状态文字自动消失
  - 引入全局 `pendingImageFiles` Map 暂存已选但未上传的文件
  - 保留旧的 `handleImageFieldUpload` 函数名作为兼容入口

#### 3. 富文本编辑器"插入图片"改造为两步流程
- 文件：`admin/admin.js` 的工具栏 `richEditorToolbarHtml` 和 `handleEditorImageSelect`、`confirmEditorImageUpload`、`clearEditorImageSelection`
- 改动：
  - "📤 插入图片" → "📁 选择图片" + "☁️ 上传并插入" + "清除选择"
  - 选择后显示文件信息；点击"上传并插入"才打开裁剪器；上传成功后才插入到编辑器
  - 引入 `pendingEditorImage` 变量暂存待上传文件

#### 4. 新增 CSS 样式
- 文件：`admin/admin.css`
- 新增：
  - `.image-field-status`：状态文字（文件名、大小、上传进度）
  - `.image-field-tag`：橙色"待上传"浮标
  - `.image-field-tag-success`：绿色"已上传"浮标
  - 调整 `.image-field-actions` 为 align-items: center

#### 5. 修复上传一直卡"上传中..."的核心 bug
- 文件：`admin/admin.js` 的 `confirmImageCrop`
- 原因：`closeImageCropper()` 会清空 `cropperCallback`，而 `confirmImageCrop` 在关闭弹窗后才调用 `cropperCallback.resolve(url)`，此时 callback 已经是 null，导致 Promise 永远 pending，前端一直显示"上传中..."
- 修复：在 `closeImageCropper()` 之前保存 `cropperCallback` 引用，并用保存的引用调用 `resolve/reject`；同时在 canvas 为空时主动 reject

#### 6. 端到端自动化测试
- 使用 Playwright 写了一个测试脚本：登录后台 → 进入案例管理 → 编辑案例 → 选择测试图片 → 点击上传 → 在裁剪器弹窗中确认 → 上传到 `/api/upload`
- 测试结果：
  - `/api/upload` 返回 `200`，返回 `{"success":true,"data":{"url":"/assets/images/uploads/..."}}`
  - hidden input `#editCaseImage` 的值正确更新为新上传的 URL
  - 预览区显示绿色"已上传"标签，状态显示 `✓ 已上传`
  - 上传按钮消失，文件真实保存到 `assets/images/uploads/`
- 已清理临时测试脚本和截图

#### 7. 最终验证
- ✓ admin.js 语法检查通过
- ✓ 数据 JSON 文件解析正常
- ✓ admin 页面 / admin.js / admin.css HTTP 200
- ✓ 服务器无需重启（静态资源直接由 Node 路由返回最新内容）
- ✓ 已无 `getRichEditorContent` 残留引用
- ✓ 端到端浏览器测试：图片上传成功并更新到表单字段

## 新增/修改文件清单
- `admin/admin.js`（重写图片字段、富文本图片流程、修复 cropper callback bug）
- `admin/admin.css`（新增 .image-field-status、.image-field-tag 样式）

---

## 头条文章图片不显示问题（用户截图反馈第二轮）

### 根本原因
- 后台"编辑头条文章"按钮操作的是 `news.featured`（保存在 news.json）
- 但前台首页 `renderArticles()` 实际读取的是 `articles.featured`（articles.json）
- 两个数据源不同步 → 用户编辑的图片前台看不到

### 修复 1：Express 路由顺序 bug
- `app.put('/api/news/featured', ...)` 和 `app.put('/api/articles/featured', ...)` 都在 `app.put('/api/news/:id', ...)` 之后
- Express 按顺序匹配第一个，把 "featured" 当成 id 去找 news，结果 404
- 修复：把两个 `/featured` 路由移到 `/:id` 之前
- 文件：`server.js`

### 修复 2：editFeaturedNews 改用 articles.featured
- `editFeaturedNews` 现在从 `/api/articles` 读取 `data.featured`（已 unwrap 一层）
- 缓存原始数据到 `window._editingFeatured` 用于保存时合并
- `currentEditType` 改为 `'article-featured'`
- 保存时 PUT `/api/articles/featured`，合并原 content/tags/category/slug 等字段

### 修复 3：恢复被破坏的 articles.json featured 内容
- 修复 2 之前的中间测试把 featured 的 content/tags/category/slug 等字段写丢失了
- 用 Node.js 脚本重写完整的 a-001 featured 对象（基于之前从 yiminbang 抓取的内容）
- content 长度 563 字符，包含：每年高考季、华侨生联考、国际生通道、海外留学、身份规划要趁早 等 5 个段落

### 修复 4：新增"从图片库选择"功能（用户的第二个需求）
- `imageFieldHtml` 加了"🖼️ 从图库选择"按钮
- 新增独立 modal `imageLibraryModal`（避免覆盖编辑器的 DOM）
- 新增函数：`openImageLibraryPicker(inputId)`、`renderImageLibraryModal(images, keyword)`、`closeImageLibrary()`、`selectFromImageLibrary(url)`
- 弹窗显示所有已上传图片（调用 `/api/images`），支持搜索过滤，点击图片即填入表单
- CSS 样式：`.image-library-grid`、`.image-library-item`、`.image-library-name`
- 关键：selectFromImageLibrary 关闭 modal 后用 setTimeout(50ms) 再写值，确保 DOM 还在

### 端到端测试结果（Playwright 自动化）
登录后台 → 新闻管理 → 点击头条"编辑" → 打开"从图片库选择" → 选图 → 关闭 → 保存
1. ✓ 库选择器正确显示 11 张已上传图片（含搜索框）
2. ✓ 选图后 hidden input `editFeaturedImage` 值正确更新
3. ✓ 保存按钮 PUT `/api/articles/featured` 返回 200
4. ✓ 重新打开编辑器：图片预览正确显示
5. ✓ 首页 `news-feature-img` 背景图正确切换到用户选的图
6. ✓ articles.json 中 featured.image 字段正确写入，所有 14 个字段（id/title/slug/category/summary/content/image/tags/status/isFeatured/createdAt/updatedAt/link/icon/gradient）齐全

### 新增/修改文件清单
- `server.js`（路由顺序调整）
- `admin/admin.js`（editFeaturedNews 重写、imageLibraryPicker 新增）
- `admin/index.html`（新增 imageLibraryModal 容器）
- `admin/admin.css`（新增 .image-library-* 样式）

### 关键截图（保存到项目根目录）
- hc-1-after-login.png：登录后页面
- hc-3-edit-modal.png：编辑头条文章 modal
- hc-4-library-picker.png：图片库选择器（11 张图片网格）
- hc-7-reopen-editor.png：保存后重新打开，图片正确显示
- hc-8-homepage.png：前台首页（含轮播 banner）

---

## 头条只显示 1 条，用户要 2 条（第三轮反馈）

### 用户反馈
- 首页「移民攻略」区块只显示 1 张头条卡片
- 后台"首页新闻 → 首页新闻推荐管理"显示的卡片标题是乱码（`以茶l弟尀岔尀尀尀尀尀尀尀尀尀中o`），但前台标题正常
- 用户需求：「图片是可以上传和显示了，但是对应的文章没有，这个位置要显示 2 条重要的文章」

### 根本原因（两个）
1. **数据源错位**：`admin/admin.js` 用 `allData.news.featured`（在 `news.json`），而首页用 `articles.featured`（在 `articles.json`）。`news.json` 中文是 GBK 编码错位成了 UTF-8 → 后台看到乱码
2. **`articles.featured` 只支持 1 条**（单对象），无法满足"2 条重要文章"需求

### 修复方案：把 `articles.featured` 升级为数组（最多 2 条）

#### 1. 数据结构改造
- `data/articles.json`：`featured` 从单对象改为 `[a-001, a-002]` 数组
- 第 1 条：a-001「高考，是孩子尤为重要的一条路，但不是唯一」
- 第 2 条：a-002「除了高考，孩子还有别的出路吗？」

#### 2. 服务端
- `GET /api/articles/featured` 新增（返回数组）
- `PUT /api/articles/featured` 改造：接受数组 payload，自动 merge 原有字段（防止部分提交导致字段丢失），限制最多 2 条
- `GET /api/articles/:id` 兼容 featured 为数组或单对象两种情况
- 文件：`server.js`

#### 3. 前台首页渲染
- `js/main.js` 的 `renderArticles`：兼容数组/对象，自动渲染 1-2 张卡片
- 1 张时单卡显示，2 张时上下叠放在左半边
- `css/style.css` 新增 `.news-feature-grid`、`.news-feature-count-1/2`、`.news-feature-tag`（红色"头条"浮标）、暗色遮罩让白字可读
- 响应式：≤992px 自动单列

#### 4. 后台管理
- `admin/admin.js` 的 `loadAllData`：之前 `allData.articles = allData.articles?.items || []` 把 `featured` 干掉了 → 改为保留 `{featured, items}` 整体结构
- 修复 `statArticles` 计数、`editArticle` 查找、`renderArticles` 等 4 处依赖 `allData.articles` 是数组的地方，全部改为 `allData.articles.items`
- `renderNews`：头条区从 1 个改为最多 2 个槽位，每个槽位独立显示 [头条 1] / [头条 2]
- `editFeaturedNews(index)`：接收 0/1 参数，加载对应槽位
- `clearFeaturedSlot(index)`：新增"清空"功能
- 保存逻辑改为：把整组 featured 数组 PUT 回去，自动补 id / slug

#### 5. 兼容老页面
- `news.html` 的 `renderFeatured`：原来 `featuredArticle = data.featured` 现在改为 `Array.isArray ? data.featured[0] : data.featured`，只取第一个作为置顶

### 端到端验证（Playwright）
- ✓ 首页显示 2 张头条卡片：标题、摘要、日期、缩略图全部正确
- ✓ 后台「首页新闻」正确显示 [头条 1] [头条 2] 两个槽位（中文乱码问题一并解决）
- ✓ 编辑头条 2 标题/摘要 → 保存 → 重新打开编辑器 → 数据正确回显
- ✓ 前台首页 头条 2 立即显示更新后的「【头条二】除了高考，还有这三条路」
- ✓ articles.json featured 是 2 项数组，所有字段完整
- ✓ 点击头条卡片正常进入文章详情页
- ✓ news.html 页面正常显示头条置顶

### 新增/修改文件清单
- `data/articles.json`（featured → 数组）
- `server.js`（PUT /articles/featured 重写、GET /articles/featured 新增、GET /articles/:id 兼容数组）
- `js/main.js`（renderArticles 支持数组、添加 tag 浮标）
- `css/style.css`（.news-feature-grid 布局、.news-feature-tag 浮标、响应式）
- `admin/admin.js`（loadAllData 保留结构、renderNews 2 槽位、editFeaturedNews 接受 index、clearFeaturedSlot 新增）
- `news.html`（renderFeatured 兼容数组）

### 关键截图（保存到项目根目录）
- `hc-2feat-FINAL-homepage.png`：首页 2 张头条卡片最终效果
- `hc-2feat-admin-news.png`：后台「首页新闻」2 槽位列表
- `hc-2feat-admin-after-save.png`：编辑后保存成功
- `hc-2feat-homepage-final.png`：保存后首页再次访问

### 经验教训
- 单对象 vs 数组的兼容性：服务端 API 接收时同时支持 `array | {items: []} | {id, ...}` 几种格式，向前兼容
- 修改 `allData.articles` 这类顶层数据时，要保持原有结构（{featured, items}），不能直接覆盖为 items 数组，否则依赖 featured 的功能全部失效
- Express 路由顺序：`/featured` 静态路由必须在 `/:id` 动态路由之前（之前修过一次，这里再次强调）

### 头条布局优化（用户反馈：头条一大图、头条二无图）
用户截图反馈：2 条头条都用了小图叠放，要求头条一恢复大图（和原 featured 一样大），头条二去掉图片。

#### 实现思路
- **整体布局**（grid `1fr 1fr`）：
  - 左半 = 头条一（大图卡 380px 高）
  - 右半 = 头条二（无图文字卡）+ 文章列表（上下叠放）
- **头条一**：`.news-feature-primary` 容器包裹 `.news-feature-card`，图片区 `height: 380px`（恢复原 featured 大小）
- **头条二**：`.news-feature-card-secondary` 横向布局，左侧 160px 渐变 + emoji 横幅，右侧文字信息
  - 完全不用 `<img>`，只用 `background: linear-gradient(...)` + `<span>emoji</span>`
  - 这样无论数据里有没有 image，都不会显示图片

#### 修改文件
- `js/main.js`（renderArticles 重写）
- `css/style.css`（新增 .news-feature-primary、.news-feature-card-secondary、.news-right-col）

#### 截图
- `hc-3feat-homepage.png`：头条一大图 + 头条二红渐变 + 列表

### 头条一附文（用户反馈：左半底部留白）
在 `.news-feature-primary` 容器内头条一大图卡下方新增紧凑型附文，填满左半空白。

#### 实现
- 数据来源：`items[0]`（普通文章列表的第一条），从列表中移除避免重复
- 样式 `.news-feature-mini`：横向布局，左侧 140px 渐变 + emoji，右侧标题+摘要+日期
- `.news-feature-primary` 改为 `flex-direction: column` + `gap: 16px`

#### 修改文件
- `js/main.js`（renderArticles 增加 miniHtml，并从 items 移除第一项）
- `css/style.css`（新增 .news-feature-mini、.news-feature-mini-icon、.news-feature-mini-info）

#### 截图
- `hc-4mini-homepage.png`：头条一大图 + 附文（紧凑型） + 头条二 + 列表

### 微信咨询 & 公众号二维码（用户反馈：所有咨询连接加上电话+微信）
用户要求：1) 文章页侧边栏咨询卡加电话+微信二维码；2) 底部加公司公众号二维码

#### 二维码资源
- 用户提供两个文件：`微信公众号二维码1.jpg`（27.5KB）、`微信公众号二维码2.png`（4.3MB 过大）
- 优化：使用 PIL 缩放 + 压缩
  - 图1: 27.5KB → 21.2KB（`wechat-qr1.jpg`，白底+蓝logo，黑白二维码）
  - 图2: 4.3MB → 63.2KB（`wechat-qr2.png`，红色二维码+粉色"微信搜一搜：汇程移民"）
- 存放路径：`assets/images/qrcode/`（统一管理）

#### 1. 文章页侧边栏重设计（`article-detail.html`）
- **结构**：标题 + 描述 → 2 个电话横排 → "或 添加微信咨询" 分隔线 → 微信二维码卡 → 立即咨询按钮
- **CSS**：`.sidebar-contact` 重写，新增 `.sc-header`、`.sc-phones`、`.sc-divider`、`.sc-qrcode`、`.sc-action`
- **亮点**：
  - 130×130 微信二维码卡（白底圆角阴影）
  - "📷 扫一扫添加微信" + "7×24 小时在线 · 1 对 1 答疑" 引导文案
  - 白色"立即咨询 →"主按钮 + 阴影

#### 2. 网站底部 footer 新增"关注我们"列（6 个页面）
- 布局：`grid-template-columns: 2fr 1fr 1fr 1fr 1.5fr`（5 列）
- 新列 `.footer-qrcode-col`：包含 2 个二维码卡片（横排）
  - 二维码1：白底 + 圆角 + 阴影 + hover 抬升效果
  - 二维码2：粉色"微信搜一搜"图，带"@汇程移民"说明
- 响应式：≤992px 改为 2 列布局，二维码区 `max-width: 320px`
- 批量更新：用 Node 脚本 `add-footer-qrcode.js` 在所有页面的"关于我们" ul 之后插入新列（避免每个页面手动改）

#### 修改文件
- `css/style.css`（footer-grid 改 5 列、新增 .footer-qrcode-*、响应式调整）
- `article-detail.html`（重设计 .sidebar-contact 样式 + HTML 结构）
- 6 个 HTML 页面（footer 新增"关注我们"列）
- `assets/images/qrcode/wechat-qr1.jpg`、`wechat-qr2.png`（图片优化后）

#### 截图
- `hc-5-footer-homepage.png`：首页底部 5 列布局 + 双二维码
- `hc-5-sidebar-article.png`：文章页侧边栏新咨询卡（含微信二维码）
- `hc-5-footer-mobile.png`：移动端 footer 响应式

---

## 2026-07-23 12:58 改造：号码放两排 + footer只留一个 + cta-box重设计

### 三个改动
1. **侧边栏号码两排显示**：`grid 1fr 1fr` 改为 `flex-direction: column`；HTML 号码补全为 `136-5185-2270` 和 `199-0172-6016`（之前被截断成 `136-5185`）
2. **footer 只留一个二维码**：删除所有页面（6 个）的"微信搜一搜 @汇程移民"那个 item，只保留"扫码关注 汇程移民"
3. **cta-box 重设计**：case-detail.html 和 project-detail.html 右侧的"获取专属方案"区，原只有按钮，改造为：标题 + 副标题 + 双号码上下叠放 + "或 添加微信咨询" 分隔线 + 微信二维码 + 立即咨询

### CSS 改造
- `.sc-phones` 改 `flex-direction: column`
- `.cta-box` padding 改 0 配合子块结构（`.cta-box-header`、`.cta-phones`、`.cta-divider`、`.cta-qrcode`）
- 复用 `wechat-qr1.jpg`（侧边栏、cta-box、footer 三处共用）
- 二维码图白底圆角阴影样式统一

### 截图
- `hc-6-sidebar-after.png`：侧边栏两排号码（完整）
- `hc-6-footer-after.png`：底部只保留一个二维码
- `hc-6-cta-after.png`：案例/项目详情页 cta-box 新设计

---

## 2026-07-23 13:30 全球移民项目空白 bug + 富文本图片库插入修复

### 用户最终反馈（Request D）
"很满意效果，网站基本正常了，只有所有国家项目都空白（点开任意国家项目都是空白）。帮我去 yiminbang.com 拉取对应项目介绍，把全球移民版块每个国家的项目介绍都写好，并且后台要有对应版块做日常更换/添加/修改，也要有丰富的富文本编辑器。网站功能都不要动了。"

### 根因 1：projects.html 缺少闭合反引号（导致全站项目空白）
- `projects.html` 第 ~283 行，`projects.map(p => { ... return \`` ... `}).join('')` 的模板字符串缺少闭合反引号
- 结果：内联 `<script>` 报 `Unexpected end of input` SyntaxError → `loadProjects()` 永不执行 → 所有国家项目页空白
- **修复**：补上 `</div>\`;` 的闭合反引号
- 验证（Playwright）：8 国项目全部正常显示：gr:1, us:3, sg:2, jp:1, nz:1, hk:2, pt:1, mt:1，无 pageerror

### 数据真相
- 12 个项目其实**早就有内容**（每个 600–1500 字符），空白完全是上面 JS 语法错误掩盖的，不是缺数据
- 仅把最短的 2 个用 Python 补全为专业全文（intro/优势/要求/流程/费用/为何选）：
  - `p-pt-001` 葡萄牙 D7 签证：673 → 1780 字
  - `p-hk-002` 香港高才通：653 → 2164 字
- 其余 10 个用户未再抱怨（之前只说"空白"）

### 根因 2：富文本编辑器"从图片库选择"插入无效
- `selectFromImageLibrary(url)` 之前无论 target 是什么都走 `setImageFieldValue(target, url)`（写给 input 字段），但富文本编辑器没有对应 input，所以图片永远插不进去
- **修复**（`admin/admin.js`）：捕获 target，若 `target === 'richEditor'` 则调用新增的 `insertImageToRichEditor(url)`：
  - 先把光标移到 `#richEditorContent` 末尾（图库选择期间选区会丢失）
  - `document.execCommand('insertHTML', ...)` 插入 `<img style="max-width:100%...">`，失败兜底 `insertAdjacentHTML`
  - 否则仍走原 `setImageFieldValue` 逻辑（图片字段不受影响）

### 富文本编辑器增强（此前已加，本轮验证）
工具栏已有 21 个按钮：段落样式/字号、加粗斜体下划线删除线、左右中对齐、有序/无序列表、清除格式、撤销重做、字体颜色/高亮、🔗链接/取消链接、📊表格、💻代码、❝引用、上下标、缩进/反缩进、分割线、📁选择图片/☁️上传并插入、🖼️从图片库选择

### 端到端验证（Playwright，已清理脚本）
1. ✓ 前台 8 国项目全部显示（无空白、无 JS 报错）
2. ✓ 后台登录 → 项目管理 12 条 → 编辑 → 富文本编辑器初始内容 1395 字符
3. ✓ 工具栏 21 按钮；图片库 15 条
4. ✓ 点"从图片库选择" → 选图 → 编辑器内 `<img>` 数量变为 1、弹窗自动关闭 ✅（关键修复生效）
5. 仅 12 个 404（浏览器每页请求 favicon.ico，站点无 favicon，无害装饰性 404，未处理）

### 修改文件清单
- `projects.html`（补闭合反引号，修复全站项目空白）
- `admin/admin.js`（selectFromImageLibrary 支持 richEditor 插入 + insertImageToRichEditor 新增）
- `data/projects.json`（p-pt-001、p-hk-002 内容补全）

### 清理
- 删除根目录 verify-editor.js、hc-*.png 截图、test-inline.html
- 删除 node 工作区 test-7*.js / test-*.js / check-js.js / inline.js / enrich-projects.py 等临时脚本
- 仅保留 logo.png

### 经验教训
- "项目空白"类问题优先用 Playwright 抓 `pageerror`：`Unexpected end of input` 几乎都是模板字符串/括号未闭合
- 复用图片库选择器时，target 语义要区分"input 字段"和"contenteditable 编辑器"，二者写入方式完全不同
- favicon.ico 缺失会在每页产生 1 个 404，可忽略或后续补一个

---

## 2026-07-23 13:45 全 12 个项目介绍二次丰富

### 用户需求
"行，在帮我把每项项目介绍再丰富一下" —— 把每个国家项目的介绍内容做厚。

### 现状评估
- 10 个项目已有完善结构（优势/条件/流程/费用），但**全部缺少「适合人群」板块**
- `p-us-003` 美国 NIW 明显偏短，且**缺失「费用概览」**段（其余项目都有）

### 改动（脚本批量，未破坏原有内容）
- 给全部 12 个项目 `content` 追加：
  1. **「适合人群」**：每项目量身定制 5 条（如希腊→预算有限/三代/房产投资/华侨生/商务；NIW→硕士/博士研究员/无需雇主/EB-1A不够/低成本）
  2. **「为什么选择汇程移民」**：统一 5 点服务优势（一对一评估/全流程透明/全球持牌律师/海外落地/长期售后）
- 仅 `p-us-003` 额外补 **「费用概览」** 段（咨询服务费/I-140/加急/NVC/制卡费等）
- `updatedAt` 全部更新为 2026-07-23

### 内容长度增量（char 数）
- p-gr-001: 1500→2017, p-us-001: →1848, p-us-002: →1946, p-sg-001: →1606, p-sg-002: →1515
- p-jp-001: →1566, p-nz-001: →1508, p-hk-001: →1690, p-pt-001: 2395, p-mt-001: 2128
- p-us-003: 800→1757（补齐费用）, p-hk-002: 2799

### 技术要点
- `server.js` 的 `readData()` 每次请求 `fs.readFileSync` 读盘 → 改 JSON 即时生效，**无需重启服务器**
- 脚本：`JSON.parse` → 遍历 `items` 追加 HTML 字符串 → `JSON.stringify(...,2)` 写回，保持格式

### 验证（Playwright）
- ✓ `/api/projects` 返回内容含「适合人群」「费用概览」
- ✓ `project-detail.html?id=p-us-003` 渲染：适合人群/费用概览/为什么选择 三段均显示，7 个 h3 板块，无 pageerror
- 已清理 enrich-all-projects.js / verify-detail.js 临时脚本

---

### 导航下拉国家项目点击无跳转（用户反馈截图）
**根因**：`index.html / projects.html / project-detail.html / news.html / article-detail.html` 的顶部导航「移民国家」下拉链接全部使用 `href="#"` 占位符；「移民项目」下拉的 4 个分类也是 `#`，点击自然不会跳转。`case-detail.html` 是唯一正确的版本。

**修改**：
- 把 5 个页面的「移民国家」下拉 6 条占位链全部改成 `projects.html?countryId=xx`，并补齐数据里已有的 8 个国家（加上 中国香港、马耳他）
- 「移民项目」下拉的 投资移民/创业移民/技术移民 指向 `projects.html?category=xx`；「购房移民」指向 `projects.html?countryId=gr`（希腊购房移民主项目）

**验证（Playwright）**
- ✓ 从 `index.html` hover「移民国家」→ 点击「希腊」→ 成功跳转到 `projects.html?countryId=gr`，渲染 1 张项目卡片，无 pageerror
- ✓ hover「移民项目」→ 点击「投资移民」→ 成功跳转到 `projects.html?category=investment`，渲染 6 张卡片
- 已清理 fix-nav-links.js / verify-nav-dropdown.js 临时脚本

**后续：用户再次截图说仍无效**
- 排查：文件与服务端内容均已正确（8 国、正确 href），JS 拦截器只作用于 `href^="#"` 的锚点链接，不会拦截新链接
- 根因：浏览器缓存了旧版 HTML；同时 Express 默认对 HTML 发 `Cache-Control: public, max-age=0` + ETag，浏览器可能不再回源
- 处理：
  - 强制结束之前无法杀掉的旧 node 进程（PID 6548），重新启动 `server.js`
  - 修改 `server.js` 的 `express.static` 配置：`etag: false`、`lastModified: false`，并对 `.html` 文件设置 `Cache-Control: no-cache, no-store, must-revalidate; Pragma: no-cache; Expires: 0`
- 验证：curl -I 确认 HTML 响应头已无 ETag/Last-Modified，Cache-Control 为 no-cache；Playwright 确认国家/项目下拉点击均能正常跳转



### 2026-07-23 15:13 再次反馈下拉点击无效（最终处理）
用户再次截图显示「移民国家」下拉仍为旧版 6 国、点击无跳转。

**根因确认**：文件与服务端已正确（8 国 + 正确 href），问题为用户端/预览窗口持续使用旧缓存 HTML。

**最终处理**：
- 在 `index.html / projects.html / project-detail.html / news.html / article-detail.html` 的 `<head>` 中加入强防缓存 meta 标签：
  - `Cache-Control: no-cache, no-store, must-revalidate`
  - `Pragma: no-cache`
  - `Expires: 0`
- 避免自动重定向脚本破坏 URL 参数，已移除之前尝试加入的检测重载脚本，仅保留 meta 标签
- 向用户提供带 `?cachebust=1` 的入口 URL，强制绕过缓存

**验证（Playwright）**
- ✓ 使用 `index.html?cachebust=${ts}` 打开首页，hover「移民国家」→ 下拉显示 8 个国家（希腊、美国、新加坡、日本、新西兰、中国香港、葡萄牙、马耳他）
- ✓ 点击「希腊」→ 跳转 URL 保留 `countryId=gr`，页面正常渲染「希腊购房移民」项目卡片
- 已清理 add-cache-busting.js / remove-redirect.js / verify-nav-final.js 临时脚本

**截图证据**
- `E:/WorkBuddy2/hcym/proof-dropdown-8.png`：下拉菜单 8 个国家
- `E:/WorkBuddy2/hcym/proof-greece-8.png`：点击希腊后正确跳转并渲染项目

---

## 头条1标签显示「教育」与后台管理不便（2026-07-23 15:24 反馈）

### 用户反馈
- 前台头条1图片左上角标签显示「教育」，与后台文章分类对不上
- 要求：后台「文章管理」里每篇文章可以一键设为「头条1」「头条2」
- 后台「新闻管理」里的头条1/头条2操作不便，且头条1"消失"

### 根因
1. `data/articles.json` 的 `featured[0]` 是一份**独立副本**，id 为 `a-002`，category 为 `education`；而链接指向的真实文章 `mrx58wv9-4al3nq` 分类是 `latest`，导致标签不一致。
2. 后台「文章管理」只有编辑/删除，没有一键置顶入口。
3. 后台「新闻管理」的头条槽位按 `featured.length` 动态显示，当只有 1 条时只显示「头条1」，用户找不到「头条2」。

### 修改

#### 1. 后台文章管理增加一键置顶按钮（`admin/admin.js`）
- `renderArticles()`：每篇文章后增加「设为头条1」「设为头条2」按钮
- 已在头条槽位的文章显示「头条1」/「头条2」高亮标签，对应按钮变「已置顶」并禁用
- 新增 `window.setArticleAsFeatured(articleId, slotIndex)`：
  - 读取文章当前字段，生成 featured 副本
  - 写入 `articles.featured[slotIndex]`（0=头条1，1=头条2）
  - 同步调用 `PUT /api/articles/:id` 把原文章 `isFeatured` 设为 `true`
  - 保存成功后刷新数据并提示

#### 2. 后台新闻管理始终显示 2 个槽位（`admin/admin.js`）
- `renderNews()`：把动态槽位改为固定 2 个，分别显示「[头条 1]」「[头条 2]」
- 空槽位提示「未设置，点击右侧编辑添加」，保留「添加」「清空」按钮

#### 3. 修正现有头条数据分类不一致
- 用脚本把 `featured[0]` 与真实文章 `mrx58wv9-4al3nq` 同步：标题、摘要、内容、图片、分类等全部一致
- 分类从 `education` 改为 `latest`，前台标签从「教育」变为「最新文章」

### 验证（Playwright）
- ✓ 后台文章列表出现「设为头条1」「设为头条2」按钮
- ✓ 点击「设为头条1」后，「新闻管理」里「[头条 1]」立即显示对应文章标题
- ✓ 首页头条区块标签显示「最新文章」，与文章实际分类一致
- 已清理 sync-featured.js / restore-featured.js / verify-featured.js / screenshot-home.js 临时脚本

### 截图证据
- `E:/WorkBuddy2/hcym/proof-article-buttons.png`：文章管理的一键置顶按钮
- `E:/WorkBuddy2/hcym/proof-news-slots.png`：新闻管理固定显示头条1/头条2
- `E:/WorkBuddy2/hcym/proof-home-featured.png`：首页头条标签已修正为「最新文章」

---

## 后台登录页泄露账号 + 上传 GitHub（2026-07-23 16:03）

### 删除泄露信息
- `admin/index.html` 第 31 行 `<p class="login-tip">默认账号：admin / huicheng2026</p>` 已删除（用户截图发现后台登录页泄露默认账号密码）

### 上传到 GitHub
- 仓库根目录在 `E:/WorkBuddy2/`（父目录），原仓库只跟踪上层 7 个文件，`hcym/` 内网站文件**从未被跟踪**
- 在 `E:/WorkBuddy2/hcym` 内新建独立 git 仓库（init empty）
- 创建 `.gitignore`：排除 `node_modules/`、`node_modules - 副本/`、`server.log`、`server.pid`、`.workbuddy/`、`*.b64`、临时文件
- 提交 73 个文件（网站全部内容，含前台/后台/data/图片）
- 远程仓库：`github.com/fengdlwxy-sudo/hcym`（main 分支）
- 旧 remote URL 内嵌的 `ghp_...` token 已失效，用户重新生成新 token 后 `--force` 推送成功
- 推送结果：`main -> main (forced update)`，远程 HEAD 与本地 commit `e831539` 一致

### 注意事项
- 旧 token 已废弃，新 token 由用户生成（不在 memory 记录）
- CNAME 文件保留（自定义域名 www.huichengyimin.com，GitHub Pages 用）
- 强制推送覆盖了旧历史，因旧仓库内容已是过时版本

---

## 新建 websit 仓库并上传完整网站（2026-07-23 16:22）

### 用户要求
- 新建独立仓库 `websit`，把 localhost:3000 当前这套完整网站全部上传
- 不能动原 `hcym` 仓库

### 操作
- 第一个 token `ghp_FJbk...` 被 GitHub 自动吊销（出现在 git remote URL 中触发安全机制，返回 `Bad credentials`）
- 用户重新生成 token `ghp_gSVO...` 后：
  - 用 GitHub API `POST /user/repos` 创建 `fengdlwxy-sudo/websit`（public）
  - JSON 含中文描述时 shell 编码报错 400，改为写入文件 `--data @file` 解决
  - `git remote add websit https://oauth2:TOKEN@...` 后 `git push -u websit main` 成功
  - 推送后立即 `git remote set-url websit https://github.com/...` 清除 token
  - 同时把 `origin`(hcym) 的 URL 里旧 token 也清除

### 验证
- websit 远程 `HEAD` = `e831539`，与本地一致
- 本地 73 文件 = 远程 73 文件
- 两个 remote 均无 token 残留

### 关键点
- GitHub 会吊销出现在 URL 中的 token，token 用完必须立即从 remote URL 清除
- 中文 JSON 走 curl 要用文件方式传，避免 shell 编码破坏

---

## GitHub Pages 显示旧版问题（用户反馈"还是旧网站"）

### 用户反馈
- 打开 `fengdlwxy-sudo.github.io/websit/` 看到的仍是旧版（教育头条、毕业帽图标）
- 需要显示和 `localhost:3000/index.html?cachebust=1` 一致的新版

### 根本原因
- 首页 `js/main.js` 默认通过 `fetch('/api/content')` 拉取 Node 后端合并数据
- GitHub Pages 是纯静态托管，没有 Node 后端，`/api/content` 返回 404
- main.js 的 catch 块 fallback 到**硬编码的默认数据**（`defaultArticles`），其中头条就是旧的教育文章
- 因此虽然仓库里的 `data/articles.json` 已更新，GitHub Pages 上仍显示旧默认内容

### 修复方案
1. **改造 `js/main.js` 的 fallback 逻辑**
   - `/api/content` 失败后，依次 fetch 静态 `data/config.json`、`data/countries.json`、`data/projects.json`、`data/articles.json`、`data/cases.json`、`data/certificates.json`
   - 每个 JSON 请求都带 `?_=${Date.now()}` 缓存破坏参数
   - 只有静态数据全部失败时才使用硬编码默认数据

2. **强制浏览器刷新 main.js**
   - 4 个 HTML 页面中 `<script src="js/main.js">` 改为 `js/main.js?v=2`
   - 避免浏览器继续使用缓存的旧 main.js

3. **更新 `.gitignore`**
   - 忽略临时截图脚本 `screenshot_*.js` 和证明图片 `websit_pages*.png`、`local_news.png`

### 推送
- 提交 2 个 commit：`fix: GitHub Pages 静态环境下从 data/*.json 加载动态内容`、`chore: ignore temporary screenshot scripts and proof images`
- push 到 `fengdlwxy-sudo/websit` 成功（用完 token 立即清除）

### 验证
- 使用 Playwright 截图 `https://fengdlwxy-sudo.github.io/websit/index.html?nocache=1`
- 头条已变为「美国移民8月排期丨EB-1前进1个月，EB-5新政仍无排期」，标签为「最新文章」
- 与本地 `localhost:3000/index.html?cachebust=1` 一致

---

## GitHub Pages 板块/文章/项目加载失败问题（用户反馈"板块的文章 图片 项目介绍 什么都没有"）

### 用户反馈
- 首页新闻区头条图片空白
- 文章详情页显示「文章加载失败」
- 项目详情页显示「项目加载失败」
- 项目列表页按国家筛选显示「加载失败，请刷新重试」

### 根本原因
1. **详情页/列表页仍直接调用 `/api/...` 接口**
   - `article-detail.html`、`project-detail.html`、`projects.html`、`case-detail.html`、`news.html` 内联脚本只请求 Node 后端 API
   - GitHub Pages 没有 Node 后端，这些接口 404，页面直接显示错误
2. **图片路径以 `/` 开头**
   - `data/articles.json`、`data/cases.json` 中上传图片路径为 `/assets/images/uploads/...`
   - 网站部署在 GitHub Pages 子目录 `/websit/` 下，以 `/` 开头的绝对路径会指向域名根目录，导致 404

### 修复方案
1. **统一静态数据 fallback**
   - 在 `js/main.js` 中新增 `resolveAssetPath()`（将 `/assets/...` 转为相对路径）和 `loadStaticData()`（加载全部 `data/*.json`）
   - 暴露全局工具：`window.hcResolveAssetPath`、`window.hcLoadStaticData`、`window.hcFetchJson`
   - 修改 `article-detail.html`、`project-detail.html`、`projects.html`、`case-detail.html`、`news.html`
     - 均引入 `js/main.js?v=2`
     - 内联脚本先尝试 `/api/...`，失败后自动 fallback 到 `data/*.json`
2. **修复所有图片路径**
   - `js/main.js` 中所有用到图片/图标的地方（轮播图、国家、服务、关于、案例、证书、头条文章）均调用 `resolveAssetPath()`
   - 详情页封面图、项目国旗等也使用 `window.hcResolveAssetPath`

### 推送
- 提交 commit：`fix: GitHub Pages 静态环境下详情页/列表页 fallback 加载 data/*.json，修复子目录部署图片路径`
- push 到 `fengdlwxy-sudo/websit` 成功（用完 token 立即清除）

### 验证
- 本地启动 server，用 Playwright 拦截 `/api/` 返回 404，模拟 GitHub Pages 环境
- 验证通过：首页头条图片显示、项目列表/详情、文章详情均正常加载
- 在线验证 `https://fengdlwxy-sudo.github.io/websit/`：
  - 首页头条图片正常显示
  - `projects.html?countryId=us` 显示 8 国筛选和美国项目卡片
  - `project-detail.html?id=p-us-001` 显示 EB-5 项目详情
  - `article-detail.html?id=mrx58wv9-4al3nq` 显示美国移民排期文章详情

### 待用户确认
- 是否将自定义域名 `www.huichengyimin.com` 从旧 `hcym` 仓库切换绑定到新 `websit` 仓库
- 若确认切换：在 websit 仓库 Pages 设置添加自定义域，并修改 DNS CNAME 指向 `fengdlwxy-sudo.github.io`

---

## 成功案例追加「为什么选择汇程移民」

### 问题
用户反馈成功案例详情页最下方缺少「为什么选择汇程移民」这段理由（项目详情页有，案例没有）。

### 修复
- 文件：`data/cases.json`
- 操作：给 5 个案例的 `content` 末尾统一追加「为什么选择汇程移民」HTML 段落
- 内容与项目详情页保持一致：资深顾问一对一评估、全流程透明服务、全球持牌律师合作、海外落地无忧、长期售后跟踪

### 验证
- 本地 `localhost:3000/case-detail.html?id=case-1` 截图验证：底部已出现「为什么选择汇程移民」段
- 线上 `https://fengdlwxy-sudo.github.io/websit/data/cases.json` 已更新（第 3 次轮询确认）

### 推送
- 提交 commit：`feat: 成功案例详情页 content 追加 为什么选择汇程移民 段`
- push 到 `fengdlwxy-sudo/websit` 成功（用完 token 立即清除）

---

## 域名切换到新版网站 www.huichengyimin.com

### 操作
- 用户要求将 `www.huichengyimin.com` 域名发布到新版 `websit` 仓库
- 检查所有仓库的 Pages cname 配置，发现 **HCYMING** 仓库占用了 `www.huichengyimin.com`
- 操作步骤：
  1. 删除 `hcym` 仓库的 CNAME 文件（避免冲突）
  2. 通过 API 清除 `HCYMING` 仓库的 cname 绑定
  3. 通过 API 在 `websit` 仓库设置 cname 为 `www.huichengyimin.com`
  4. 触发 `websit` Pages 重建

### 验证
- GitHub API 返回 `cname: www.huichengyimin.com`，`html_url: https://www.huichengyimin.com/`，`https_enforced: true`
- 访问 `https://www.huichengyimin.com/` 已显示新版网站
- Playwright 验证：头条为「美国移民8月排期丨EB-1前进1个月，EB-5新政仍无排期」，标签「最新文章」

### 注意
- 用户账户下有 4 个仓库开启 Pages：`hcym`、`HCYMING`、`web`、`websit`
- 域名占用冲突是问题的根因，单纯删除 CNAME 文件不够，必须通过 API 清除占用仓库的 cname 字段
