# 依赖（体积大，不入库）
node_modules/
node_modules - 副本/

# 运行日志 / 进程文件
server.log
server.pid

# 环境变量（含密码等敏感信息）
.env

# 用户上传的图片（体积大，运行时生成；保留目录结构）
assets/images/uploads/*
!assets/images/uploads/.gitkeep

# 系统 / 编辑器临时文件
.DS_Store
Thumbs.db
*.swp
